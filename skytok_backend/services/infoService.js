const db = require('./db.js');
const infoModel = require('../models/info.js');

let infoService = {
    send: function (req, res) {
        // let infos = req.body.deviceInfo;
        // infoModel.create(infos, function (err, info) {
        //     if (err) {
        //         console.log(err);
        //     } 
        // }
        // );
    }
}

module.exports = infoService;